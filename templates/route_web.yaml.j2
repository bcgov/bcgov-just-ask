apiVersion: route.openshift.io/v1
kind: Route
metadata:
  creationTimestamp: null
  labels:
    app: "{{ web.name }}"
    group: "{{ app_name }}"
  name: "{{ web.name }}"
spec:
  # host defaults to <name>.<namespace>.apps.silver.gov.bc.ca
  port:
    targetPort: "{{ web.port }}-{{ web.port }}"
  to:
    kind: ""
    name: "{{ web.name }}"
    weight: null
  tls:
    termination: Edge
    insecureEdgeTerminationPolicy: Redirect
status: {}