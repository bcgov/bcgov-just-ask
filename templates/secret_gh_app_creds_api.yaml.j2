apiVersion: v1
stringData:
  CLIENT_SECRET: "{{ CLIENT_SECRET }}"
  CLIENT_ID: "{{ CLIENT_ID }}"
  APP_ID: "{{ APP_ID }}"
kind: Secret
metadata:
  name: "{{ api.name }}-client-credentials"
  labels:
    app: "{{ api.name }}"
    group: "{{ app_name }}"